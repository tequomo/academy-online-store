export type FilterCamera = {
  type: string,
  matrix_resolution: number | string,
  supporting: string,
};

export type FilterCar = {

};

export type FilterEstate = {
  type: string,
  area: number,
  rooms_count: number,
};

export type FilterLaptop = {
  type: string,
  ram_value: number | string,
  screen_size: number | string,
  cpu_type: string,
};
